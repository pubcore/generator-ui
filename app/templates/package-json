{
  "name": "<%= name %>",
  "version": "1.0.0",
  "description": "<%= description %>",
  "main": "js/index.js",
  "scripts": {
    "postinstall": "npm run copyFontFiles && npm run build",
    "build": "npm run js:build && npm run sass:build",
    "js:build": "webpack --mode production && npm run js:build-node",
    "js:watch": "webpack --mode development --watch",
    "js:build-node": "babel src-node --out-dir js",
    "js:watch-node": "babel --watch src-node --out-dir js",
    "sass:build": "node-sass --include-path node_modules/ --output-style compressed --source-map htdocs/css/ -o htdocs/css/ scss/",
    "sass:watch": "node-sass --watch --include-path node_modules/ --source-map-embed -o htdocs/css/ scss/",
    "watch": "npm run sass:watch | npm run js:watch",
    "copyFontFiles": "ncp node_modules/@fortawesome/fontawesome-free/webfonts htdocs/fonts"
  },
  "license": "MIT",
  "babel": {
    "presets": [
      "@babel/preset-env"
    ],
    "plugins": [
      "@babel/plugin-proposal-object-rest-spread"
    ]
  }
}
