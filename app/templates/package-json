{
  "name": "<%= name %>",
  "version": "1.0.0",
  "description": "<%= description %>",
  "main": "js/index.js",
  "scripts": {
    "postinstall": "npm run copyFontFiles && npm run build",
    "build": "webpack --mode production",
    "watch": "webpack --mode development --watch",
    "copyFontFiles": "ncp node_modules/@fortawesome/fontawesome-free/webfonts htdocs/fonts"
  },
  "license": "MIT",
  "babel": {
    "presets": [
      "@babel/preset-env"
    ],
    "plugins": [
      "@babel/plugin-proposal-object-rest-spread"
    ]
  }
}
